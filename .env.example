# Skill Evolution Registry — Environment Variables
# Copy this file to .env in your project root.
#
# The public registry is pre-configured — search/install/publish work out of the box.
# Only change SUPABASE_URL/SUPABASE_ANON_KEY if you want to run a private registry.

# Public registry (default — no changes needed)
SUPABASE_URL=https://ptwosnmrcfwmfnluufww.supabase.co
SUPABASE_ANON_KEY=sb_publishable_BNikqoeKRZEp2FSqAOJu4A_pJ0UBzby

# Auto-generated on first publish — binds your author identity.
# If lost, ask the registry admin to reset via reset_publisher_key RPC.
# PUBLISHER_KEY=

# NOT needed for normal use — only for registry admin operations
# SUPABASE_SERVICE_KEY=

# --- Embedding (optional) ---
# Enables semantic search and embedding on publish.
# Set ANY ONE of the following keys — the first available is used.
# Without a key, search falls back to full-text search (still works fine).
#
# Option 1: DashScope (Alibaba Cloud) — https://dashscope.console.aliyun.com/
# DASHSCOPE_API_KEY=
#
# Option 2: SiliconFlow (free BAAI/bge-m3 model) — https://siliconflow.cn/
# SILICONFLOW_API_KEY=
#
# Option 3: OpenAI or any compatible API
# OPENAI_API_KEY=
# EMBEDDING_BASE_URL=https://api.openai.com/v1/embeddings
